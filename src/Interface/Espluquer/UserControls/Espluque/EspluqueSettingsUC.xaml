<UserControl x:Class="Espluquer.UserControls.Espluque.EspluqueSettingsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="500">

    <Grid Background="{DynamicResource App.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource App.Surface}"
                BorderBrush="{DynamicResource App.Border}"
                BorderThickness="0,0,0,1"
                Padding="2,6">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Style="{DynamicResource App.FluentButton}"
                        Content="&#xF42F;"
                        Click="OpenFileLocationButton_Click" />

                <TextBox Grid.Column="1"
                         x:Name="FilePathTextbox"
                         Style="{DynamicResource App.ReadOnlyTextBox}" />
            </Grid>
        </Border>

        <Grid Grid.Row="1"
              Margin="24">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="0,0,0,16">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Database file"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource App.Text}" />

                <TextBox Grid.Column="1"
                         Text="{Binding Db, Mode=TwoWay}"
                         MinWidth="300"
                         Padding="8,5"
                         Background="{DynamicResource App.Surface}"
                         Foreground="{DynamicResource App.Text}"
                         BorderBrush="{DynamicResource App.Border}" />
            </Grid>

            <Grid Grid.Row="1"
                  Margin="0,0,0,16">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Log file path"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource App.Text}" />

                <TextBox Grid.Column="1"
                         Text="{Binding LogFilePath, Mode=TwoWay}"
                         MinWidth="300"
                         Padding="8,5"
                         Background="{DynamicResource App.Surface}"
                         Foreground="{DynamicResource App.Text}"
                         BorderBrush="{DynamicResource App.Border}" />
            </Grid>

            <Grid Grid.Row="2"
                  Margin="0,0,0,16">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Theme"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource App.Text}" />

                <ComboBox Grid.Column="1"
                          Style="{StaticResource App.StandardComboBox}"
                          SelectedValue="{Binding Theme, Mode=TwoWay}"
                          SelectedValuePath="Content"
                          MinWidth="300"
                          Padding="8,5" >

                    <ComboBoxItem Content="Dark" />
                    <ComboBoxItem Content="Light" />
                    <ComboBoxItem Content="HighContrast" />
                </ComboBox>
            </Grid>

            <Grid Grid.Row="3">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Recent files"
                           VerticalAlignment="Top"
                           Foreground="{DynamicResource App.Text}" />

                <TextBox Grid.Column="1"
                         Text="{Binding RecentFilesText, Mode=TwoWay}"
                         MinWidth="300"
                         MinHeight="150"
                         Padding="8,5"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Background="{DynamicResource App.Surface}"
                         Foreground="{DynamicResource App.Text}"
                         BorderBrush="{DynamicResource App.Border}" />
            </Grid>


        </Grid>
    </Grid>
</UserControl>