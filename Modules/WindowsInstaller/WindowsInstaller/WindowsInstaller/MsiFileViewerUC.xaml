<UserControl x:Class="WindowsInstaller.MsiFileViewerUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TreeView x:Name="FileTreeView"
          Grid.Column="0"
          Style="{DynamicResource App.Controls.TreeView}"
          SelectedItemChanged="FileTreeView_SelectedItemChanged">

            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem"
               BasedOn="{StaticResource App.Controls.TreeViewItem}">
                    <Setter Property="IsExpanded" Value="True" />
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding BranchChildren}">
                    <TextBlock Text="{Binding Name}"
                       Style="{StaticResource App.StandardTextBlock}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

        </TreeView>

        <GridSplitter Grid.Column="1"
                      Width="3"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="{DynamicResource App.Border}" />

        <DataGrid x:Name="FileDataGrid"
                  Grid.Column="2"
                  Style="{DynamicResource App.Controls.DataGrid}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  HeadersVisibility="Column"
                  MouseDoubleClick="FileDataGrid_MouseDoubleClick">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Name" Binding="{Binding TargetName}" />
                <DataGridTextColumn Header="Size" Binding="{Binding FileSize}" />
                <DataGridTextColumn Header="Version" Binding="{Binding FileVersion}" />
                <DataGridTextColumn Header="Component" Binding="{Binding ComponentKey}" />
                <DataGridTextColumn Header="Sequence" Binding="{Binding Sequence}" />
                <DataGridTextColumn Header="Attributes" Binding="{Binding Attributes}" />

            </DataGrid.Columns>

        </DataGrid>

        <Grid x:Name="LoadingPanel" Grid.ColumnSpan="3" Panel.ZIndex="10">
            <Grid Width="120" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="LoadingText" Text="Loading" HorizontalAlignment="Left" Style="{StaticResource App.DropMessageText}" />
            </Grid>
        </Grid>
    </Grid>
</UserControl>