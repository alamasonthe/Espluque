<UserControl x:Class="RegViewer.RegEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:theme="clr-namespace:Espluque.Theming;assembly=Espluque.Theming"
             xmlns:local="clr-namespace:RegViewer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TreeView x:Name="ContainerTreeView"
          Grid.Column="0"
          Style="{DynamicResource App.Controls.TreeView}"
          SelectedItemChanged="ContainerTreeView_SelectedItemChanged">

            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem"
               BasedOn="{StaticResource App.Controls.TreeViewItem}">
                    <Setter Property="IsExpanded" Value="True" />
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding BranchChildren}">
                    <TextBlock Text="{Binding Name}"
                       Style="{StaticResource App.StandardTextBlock}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

        </TreeView>

        <GridSplitter Grid.Column="1"
                  Width="3"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Background="{DynamicResource App.Border}"/>

        <RichTextBox x:Name="LeafRichTextBox"
             Grid.Column="2"
             IsReadOnly="True"
             IsDocumentEnabled="False"
             BorderThickness="0"
             Background="{DynamicResource App.Background}"
             Foreground="{DynamicResource App.Text}"
             SelectionBrush="{DynamicResource App.Selection}"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Disabled"
             Padding="0"
             FontFamily="Segoe UI"
             FontSize="13" />

    </Grid>
</UserControl>
